# Core runtime dependencies (aligned with pyproject.toml).
# For optional stacks:
# - ML models: pip install -e ".[ml]"
# - MLOps stack: pip install -e ".[ops]"
# - Dev tools: pip install -e ".[dev]"
numpy
pandas
scikit-learn>=1.2,<2
matplotlib
seaborn
joblib
scipy
pyyaml
pydantic>=2,<3
tenacity
